# go-mcp-postgres-server configuration
# Copy this file to .env and fill in your values.
# Environment variables take priority over values in this file.

MCP_DB_HOST=localhost
MCP_DB_PORT=5432
MCP_DB_USER=postgres
MCP_DB_PASSWORD=changeme
MCP_DB_NAME=mcp_db
MCP_LISTEN_ADDR=0.0.0.0:5353
